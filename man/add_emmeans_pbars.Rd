% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_emmeans_pbars.R
\name{add_emmeans_pbars}
\alias{add_emmeans_pbars}
\title{Add emmeans pairwise comparison brackets to a ggplot}
\usage{
add_emmeans_pbars(
  p,
  contrasts,
  y_offset = 0.05,
  step.increase = 0.08,
  dodge_width = 0.75,
  label = c("stars", "p.format", "p.value"),
  hide.ns = TRUE,
  y_col = NULL,
  ...
)
}
\arguments{
\item{p}{A ggplot object. Must contain data and define both x and y
aesthetics in the main \code{ggplot(aes(...))} call.}

\item{contrasts}{An object returned by \code{emmeans::pairs()} or
\code{emmeans::contrast()}. Must contain a \code{contrast} column and a \code{p.value}
column after conversion with \code{\link[=as.data.frame]{as.data.frame()}}.}

\item{y_offset}{Numeric. Fraction of the local y-range used to place the
first significance bracket above the data. Default is \code{0.05}.}

\item{step.increase}{Numeric. Fraction of the local y-range used to
vertically separate multiple brackets within the same panel or grouping
context. Default is \code{0.08}.}

\item{dodge_width}{Numeric. Width used to calculate bracket positions for
dodged geoms, such as \code{geom_boxplot(aes(fill = group))}. Default is
\code{0.75}, which matches the default boxplot dodge width.}

\item{label}{Character. Label style for the brackets. One of:
\itemize{
\item \code{"stars"} for significance stars (default)
\item \code{"p.format"} for formatted p-values
\item \code{"p.value"} for raw p-values
}}

\item{hide.ns}{Logical. If \code{TRUE} (default), nonsignificant comparisons
(p >= 0.05) are removed before plotting.}

\item{y_col}{Character. The name of the column in the ggplot data
containing the group means (y-axis values). If \code{NULL} (default), the
means column is inferred from the plot's y aesthetic.}

\item{...}{Additional arguments passed to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}.}
}
\value{
A ggplot object with significance brackets added.
}
\description{
\code{add_emmeans_pbars()} adds significance brackets to an existing ggplot using
pairwise comparisons generated by \code{emmeans::pairs()} or \code{emmeans::contrast()}.
It is designed to make model-derived post hoc comparisons easier to display
on ggplots, especially when plots include facets, nested facets, fill/color
aesthetics, or dodged geoms.
}
\details{
The function uses the ggplot object to infer the x aesthetic, y aesthetic,
fill/color aesthetics, facet variables, dodged group positions, and
panel-specific y ranges. It then uses the emmeans contrast table to infer
group1, group2, p.value, and comparison context, and passes a cleaned
annotation table to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}.

\strong{Recommended emmeans pattern:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{emmeans(model, ~ variable_being_compared | variables_that_define_context) |>
  pairs()
}\if{html}{\out{</div>}}

Variables after \code{|} should include everything needed to identify where each
comparison belongs in the plot (facet variables, x-axis context, etc.).

The function supports these common plotting structures:
\enumerate{
\item \strong{Simple x-axis comparisons.}
\code{ggplot(aes(Cell, Area))}
\item \strong{Faceted x-axis comparisons.}
\code{ggplot(aes(Cell, Area)) + facet_wrap(~ Spheroid)}
\item \strong{Multiple or nested facets.}
\code{facet_nested_wrap(~ Spheroid + Matrix)}
\item \strong{Dodged fill/color plots.}
\code{ggplot(aes(Cell, Area, fill = Spheroid))}
\item \strong{Fill/color comparisons within an x-axis category.}
\code{ggplot(aes(Spheroid, Area, fill = Cell))}
}

For best results the emmeans structure should mirror the plot structure:
the variable being compared appears before \code{|}, and variables defining
facets, dodges, or panel context appear after \code{|}.
}
\examples{
\dontrun{
# Simple x-axis comparison
emm <- emmeans(fit, ~ Cell) |> pairs()
p   <- df |> ggplot(aes(Cell, Area)) + geom_boxplot()
add_emmeans_pbars(p, emm)

# Faceted comparison
emm <- emmeans(fit, ~ Cell | Spheroid) |> pairs()
p   <- df |>
  ggplot(aes(Cell, Area)) +
  geom_boxplot() +
  facet_wrap(~ Spheroid)
add_emmeans_pbars(p, emm)

# Dodged fill comparison
emm <- emmeans(fit, ~ Cell | Spheroid) |> pairs()
p   <- df |> ggplot(aes(Cell, Area, fill = Spheroid)) + geom_boxplot()
add_emmeans_pbars(p, emm)

# Fill-group comparison within each x-axis level
emm <- emmeans(fit, ~ Cell | Spheroid) |> pairs()
p   <- df |> ggplot(aes(Spheroid, Area, fill = Cell)) + geom_boxplot()
add_emmeans_pbars(p, emm)

# Adjust bracket placement
add_emmeans_pbars(p, emm, y_offset = 0.10, step.increase = 0.12)

# Show formatted p-values instead of stars
add_emmeans_pbars(p, emm, label = "p.format")

# Show all comparisons including nonsignificant
add_emmeans_pbars(p, emm, hide.ns = FALSE)

# Match a custom dodge width
p2 <- df |>
  ggplot(aes(Cell, Area, fill = Spheroid)) +
  geom_boxplot(position = position_dodge(width = 0.8))
add_emmeans_pbars(p2, emm, dodge_width = 0.8)
}

}
